{
  "name": "@hyrious/esbuild-plugin-commonjs",
  "version": "0.1.0",
  "description": "The @rollup/plugin-commonjs as esbuild plugin.",
  "main": "index.js",
  "keywords": [
    "esbuild",
    "plugin",
    "commonjs",
    "modules",
    "require"
  ],
  "author": "hyrious <hyrious@outlook.com>",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --external:esbuild --external:acorn --platform=node --target=node12 --outfile=index.js --sourcemap --sources-content=false index.ts",
    "test": "esbuild-dev index.test.ts"
  },
  "peerDependencies": {
    "esbuild": "*"
  },
  "dependencies": {
    "@rollup/plugin-commonjs": "^21.0.1",
    "acorn": "^8.7.0"
  },
  "devDependencies": {
    "@hyrious/esbuild-dev": "^0.7.1",
    "@types/node": "^17.0.8",
    "esbuild": "^0.14.10"
  }
}
